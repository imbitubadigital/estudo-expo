import React from 'react';
import { View, Text } from 'react-native';

import { Container } from './styles';

export default function ListRestaurants({ restaurants }) {
  return (
    <View>
      <Text>Lista de resutarantes</Text>
    </View>
  );
}
